{
  "version": 2,
  "builds": [
    { "src": "api/**/*.cjs", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.cjs" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
